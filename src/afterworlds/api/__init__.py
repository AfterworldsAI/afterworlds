"""Minimal FastAPI API surface — CRD Issue 19.

Route handlers translate HTTP <-> typed service calls only (Binding Decision
2). No orchestration, provider, entitlement, mode, intent, or option-
resolution policy lives in this package.
"""
